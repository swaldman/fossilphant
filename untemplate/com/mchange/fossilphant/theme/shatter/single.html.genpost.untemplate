val context = lpwc.context

val location = lpwc.siteRootedLocation

val content = threadOrPost( lpwc, Some(linkOut) )

val tagline = context.tagline

val input = MainLayoutInput( location, context.mainTitle, tagline, topnav_single_html(lpwc).text, content, "", context.endOfHeadExtraHtml, context.endOfBodyExtraHtml  )

(lpwc : LocatedPostWithContext)[]~()>

<( layout_main_html( input ) )>


